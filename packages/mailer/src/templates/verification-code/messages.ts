/* tslint:disable:max-line-length */
import { defineMessages } from '@er/intl'

export default defineMessages({
  subject: {
    id: 'mailer.verification-code.subject',
    defaultMessage: 'Код подтверждения',
  },
})
